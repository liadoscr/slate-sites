# Supabase: create a project, then copy these values from Project Connect.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Server-only. Never expose this value to the browser or commit it.
SUPABASE_SERVICE_ROLE_KEY=

# Used by Slate's backend when it creates a short-lived, one-time handoff.
SLATE_HANDOFF_HMAC_SECRET=

# Server-side application URL used for Slate handoffs. Use http://localhost:3000 locally.
SLATE_SITES_APP_URL=http://localhost:3000

# Gemini is server-only. Do not use a NEXT_PUBLIC_ prefix.
GEMINI_API_KEY=
# Optional: override the lightweight default model for testing.
GEMINI_MODEL=gemini-3.1-flash-lite

# Optional automatic stock photos for description-first creation. Server-only.
# Create an API key at https://www.pexels.com/api/ (never NEXT_PUBLIC_).
PEXELS_API_KEY=

# Optional lead-email notifications. Server-only; use a verified Resend sender.
# In production SLATE_SITES_APP_URL must be https://slate-sites.vercel.app
RESEND_API_KEY=
LEAD_EMAIL_FROM=
# Bearer secret for /api/cron/lead-notifications. Configure an external scheduler
# to call it every 5 minutes; the endpoint does not schedule itself.
CRON_SECRET=
